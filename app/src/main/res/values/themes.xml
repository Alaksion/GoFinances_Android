<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GoFinances" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/app_purple</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.GoFinances.Fullscreen" parent="Theme.GoFinances">
        <item name="android:actionBarStyle">@style/Widget.Theme.GoFinances.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.GoFinances.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>

    <style name="WindowAnimations">
        <item name="android:activityOpenEnterAnimation">
            @anim/act_enter</item>
        <item name="android:activityOpenExitAnimation">
            @anim/act_exit</item>
        <item name="android:activityCloseEnterAnimation">
            @anim/act_enter</item>
        <item name="android:activityCloseExitAnimation">
            @anim/act_exit</item>
    </style>
</resources>